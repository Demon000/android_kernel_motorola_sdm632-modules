# add -Wall to try to catch everything we can.
EXTRA_CFLAGS += -Wall
EXTRA_CFLAGS += -I$(KERNEL_SRC)/../sdm632-modules/motorola/include \
		-I$(KERNEL_SRC)/../sdm632-modules/motorola/drivers/power/mmi_pl_chg_manager

obj-m += mmi_pl_chg_manager.o
